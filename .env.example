# MiniNAS Configuration

# Volumes are managed via CLI: pnpm mininas volume add <id> <label> <path>

# Server port
PORT=3001

# Session secret (generate with: openssl rand -hex 32)
SESSION_SECRET=change-me-to-a-random-secret

# CLI secret for authenticating CLI commands (generate with: openssl rand -hex 32)
CLI_SECRET=change-me-to-a-random-secret

# WebAuthn Relying Party configuration
RP_ID=localhost
RP_NAME=MiniNAS
RP_ORIGIN=http://localhost:4321

# Base URL for Tailscale/external access (used for WebAuthn RP derivation)
# RP_ID and RP_ORIGIN are auto-derived from this. Example: https://mininas.tail1234.ts.net
# BASE_URL=

# Base path prefix for sub-path mounting (e.g., /mininas)
# All routes will be served under this path. Leave empty for root.
# BASE_PATH=

# Public share server port (for Tailscale Funnel). Set to enable public file sharing.
# PUBLIC_SHARE_PORT=3002

# Public URL for share links accessible outside the Tailnet
# Example: https://mininas.tail1234.ts.net:8443
# PUBLIC_SHARE_URL=

# SQLite database path (default: ~/.mininas/data/mininas.db)
# DB_PATH=~/.mininas/data/mininas.db

# Thumbnail cache directory (default: ~/.mininas/data/thumbnails)
# THUMBNAIL_DIR=~/.mininas/data/thumbnails

# Upload staging directory (default: ~/.mininas/data/uploads)
# UPLOAD_STAGING_DIR=~/.mininas/data/uploads

# Custom display name shown in the UI header and page titles (default: MiniNAS)
# APP_NAME=MiniNAS
