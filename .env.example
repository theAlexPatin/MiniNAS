# MiniNAS Configuration

# Comma-separated list of volume mount paths
# Each entry: id:label:path
VOLUMES=ssd1:SSD 1:/Volumes/SSD1,ssd2:SSD 2:/Volumes/SSD2

# Server port
PORT=3001

# Session secret (generate with: openssl rand -hex 32)
SESSION_SECRET=change-me-to-a-random-secret

# WebAuthn Relying Party configuration
RP_ID=localhost
RP_NAME=MiniNAS
RP_ORIGIN=http://localhost:4321

# SQLite database path
DB_PATH=./data/mininas.db

# Thumbnail cache directory
THUMBNAIL_DIR=./.mininas/thumbnails

# Upload staging directory
UPLOAD_STAGING_DIR=./.mininas/uploads
